Create a view `{schema}.t11_route_km(city text, km int)`: the shortest driving distance from {origin_city} to every other reachable city. `roads(src, dst, km)` are two-way but each road is stored once. New roads open regularly, so this should be a maintained view.
