Create a view `{schema}.t02_depth(employee_id int, depth int)` giving the depth of every employee under the CEO (employee {ceo_id}), with the CEO at depth 0. Only include employees reachable from the CEO.
