Create a view `{schema}.t08_scc(account_id text, component text)` assigning every account to its strongly connected component in the transfers graph, labelled by the smallest account id in the component. Every account gets a row.
