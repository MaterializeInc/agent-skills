Create a view `{schema}.t01_descendants(employee_id int)` listing every employee who reports, directly or through any chain of managers, to employee {loop_manager}. Do not include employee {loop_manager}. The `employees` table has `id, manager_id, name, salary`. Some of the manager data is known to be dirty.
