A colleague wrote this view and it never returns a result. Fix it and create it as `{schema}.t14_reachable(src text, dst text)`, the set of (src, dst) account pairs where money can flow from src to dst through one or more transfers:

```
CREATE VIEW t14_reachable AS
WITH MUTUALLY RECURSIVE
    reach(src text, dst text) AS (
        SELECT src, dst FROM transfers
        UNION ALL
        SELECT src, dst FROM reach
        UNION ALL
        SELECT r.src, t.dst FROM reach r JOIN transfers t ON t.src = r.dst
    )
SELECT src, dst FROM reach;
```

Explain in one paragraph why it never returned.
