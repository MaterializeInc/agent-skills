The flagged account is {flagged_account}. I want all the paths money can take from it in up to {hops} transfers. Create a view `{schema}.t06_within_hops(account_id text)` listing every other account that money from the flagged account can reach within {hops} transfers along `transfers(src, dst, amount, ts)`.
