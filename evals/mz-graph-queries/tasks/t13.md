Create a view `{schema}.t13_downstream(task_id text)`: every pipeline task that would be affected, directly or indirectly, if {impact_task} produced bad data. `depends_on(task, prereq)` says task needs prereq. Exclude {impact_task} itself.
