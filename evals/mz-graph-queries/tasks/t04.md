Create a view `{schema}.t04_kit_quantity(part_id int, qty int)`: for one unit of part {kit_part}, how many of each other part are needed in total. `bom(parent_id, child_id, qty)` says a parent needs qty of child. Some components are used under more than one assembly.
